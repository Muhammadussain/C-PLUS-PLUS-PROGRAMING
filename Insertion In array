#include <iostream>
using namespace std;
int Winsert (int LA[], int n, int pos, int item)
{
for (int i=n; i>pos; i--)
{
LA[i]=LA[i-1];
}
LA[pos]=item;
}
int main()
{
int a[]={1,2,3,4,5};
int soa=sizeof(a)/sizeof(int);
Winsert(a,soa,2,69);
for(int i=0; i<soa+1; i++)
{
cout<<a[i]<<",";
}
Return 0;
}
